[Unit]
Description=swdice
After=network.target
[Service]
ExecStart=/root/swdice-discord/sw-dice.js
#Restart=always
User=root
Group=root
Environment=PATH=/usr/bin:/usr/local/bin
Environment=NODE_ENV=production
WorkingDirectory=/root/swdice-discord
[Install]
WantedBy=multi-user.target
